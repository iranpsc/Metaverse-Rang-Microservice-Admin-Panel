name: "Metaverse Admin CodeQL config"

# Static assets under public/assets (fonts, images, Persian datepicker) are not
# application source and are excluded from CodeQL analysis.
paths-ignore:
  - public/assets
